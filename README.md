# OMG-WOW
This plan details the setup and development of three interactive RAG developer projects under the d:/OMG WOW directory. Each project represents a highly hireable portfolio piece, built using Streamlit (Python) and styled with custom premium CSS rules. They will all support both Simulation Mode (runs immediately offline with mock datasets, ch
